<%= simple_form_for(@event_invite) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :status %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
