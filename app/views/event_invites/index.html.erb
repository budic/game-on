<h1>Listing event_invites</h1>

<table>
  <thead>
    <tr>
      <th>Status</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @event_invites.each do |event_invite| %>
      <tr>
        <td><%= event_invite.status %></td>
        <td><%= link_to 'Show', event_invite %></td>
        <td><%= link_to 'Edit', edit_event_invite_path(event_invite) %></td>
        <td><%= link_to 'Destroy', event_invite, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event invite', new_event_invite_path %>
