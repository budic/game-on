<% @location.recurring_events.each do | r | %>
  <%= link_to r do %>
  <div style='position:relative; background-color:#efe; padding: 3px'>
    <span class='linkedspan'></span>       
    <span>
      <strong>
        <%= link_to Date::DAYNAMES[ r.day ],  r.location.name  %> 
        <%= " - " + r.game_type.name %> 
      </strong>
    </span> 
    <br>
    <%= r.name %>
    <% time = "" %>
    <%if r.start_time %>
    <% time += r.start_time.strftime("%l:%M %P") %>
      <% if r.end_time %>
      <%time += " - " + r.end_time.strftime("%l:%M %P") %>
      <% end %>
    <% end %> 
    <div > <%= time %> </div>
    <span class="stars"> <%= r.level %></span> 
  </div>
  <br>
  <% end %>
<% end %>


